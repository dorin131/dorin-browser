{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Browser",
            "command": "/usr/bin/cmake --build /home/dorin/dev/dorin-browser/build --config Debug --target all -- -j 6",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}